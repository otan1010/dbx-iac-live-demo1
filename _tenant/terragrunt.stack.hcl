#Should contain tenant level objects such as EntraId users, groups, service principals, applications
#Since Databricks "account" has a 1-1 relationship with a tenant (i.e. one account per tenant)
#the global Databricks account configuration should also be done here
